[gd_scene format=3 uid="uid://cprshellscn01"]

[ext_resource type="Script" uid="uid://xqi7kv2y70eo" path="res://addons/godot_editable_texture/editable_texture.gd" id="editable_texture_script"]
[ext_resource type="PackedScene" path="res://ui/dialogue_box.tscn" id="9_dialogue"]
[ext_resource type="PackedScene" path="res://ui/overhead_typewriter.tscn" id="12_overhead"]
[ext_resource type="FontFile" uid="uid://bajmgrsnfc8be" path="res://ui/fonts/fusion-pixel-8px-zh_hans.woff2" id="15_font"]
[ext_resource type="AudioStream" uid="uid://ob7skrbgnusm" path="res://audio/the_one_who_stands_distant.ogg" id="19_home_day_music"]
[ext_resource type="AudioStream" uid="uid://c86wv21js45cy" path="res://audio/majesty.ogg" id="21_king_day_music"]
[ext_resource type="AudioStream" uid="uid://o7js72pawqgm" path="res://audio/muffled_dirt_thud_a.wav" id="22_dirt_a"]
[ext_resource type="AudioStream" uid="uid://cb5s0bvsmamf5" path="res://audio/muffled_dirt_thud_b.wav" id="23_dirt_b"]
[ext_resource type="AudioStream" uid="uid://vcbguj00jy2s" path="res://audio/dry_grass_rustle_a.wav" id="24_grass_a"]
[ext_resource type="AudioStream" uid="uid://cuivhluijhgxn" path="res://audio/dry_grass_rustle_b.wav" id="25_grass_b"]

[sub_resource type="Shader" id="Shader_palette"]
code = "shader_type canvas_item;

uniform sampler2D palette : source_color, filter_nearest;

void fragment() {
	vec4 source = texture(TEXTURE, UV);
	if (source.a > 0.0) {
		float closest_distance = 4.0; // Greater than the maximum squared RGB distance (3.0).
		vec3 closest_color = source.rgb;
		ivec2 palette_size = textureSize(palette, 0);
		int palette_width = palette_size.x;
		int palette_height = palette_size.y;
		for (int y = 0; y < palette_height; y++)
		{
			float palette_v = (float(y) + 0.5) / float(palette_height);
			for (int x = 0; x < palette_width; x++) {
				float palette_u = (float(x) + 0.5) / float(palette_width);
				vec3 palette_color = texture(palette, vec2(palette_u, palette_v)).rgb;
				float dis = distance(source.rgb, palette_color);
				if (dis < closest_distance) {
					closest_distance = dis;
					closest_color = palette_color;
				}
			}
		}
		COLOR = vec4(closest_color, source.a);
	} else {
		COLOR = source;
	}
}
"

[sub_resource type="Texture2D" id="EditableTexture_palette"]
script = ExtResource("editable_texture_script")
_base64_data = ""

[sub_resource type="GDScript" id="GDScript_shell"]
script/source = "extends Control
## 星球运行壳：256×224 视口、玩家、UI、输入与配乐。
## 视口拉伸、像素过滤等静态配置写在 planet_run_shell.tscn。

const META_OPENING_DAY_MUSIC := \"opening_day_music\"

@onready var game_viewport: SubViewport = $GameView/GameViewport
@onready var planet = $GameView/GameViewport/Planet
@onready var player = $GameView/GameViewport/Player
@onready var dialogue = %DialogueBox


func _ready() -> void:
	planet.teleport_player(planet.spawn_angle)
	var story := planet.get_node(\"%Story\") as PlanetStory
	if story.auto_start:
		story.start()


func replace_planet(next_planet: Node2D) -> void:
	var previous_planet: Node2D = planet
	next_planet.name = \"Planet\"
	next_planet.unique_name_in_owner = true
	var planet_index := previous_planet.get_index()
	previous_planet.unique_name_in_owner = false
	previous_planet.name = \"LeavingPlanet\"
	game_viewport.add_child(next_planet)
	next_planet.owner = self
	game_viewport.move_child(next_planet, planet_index)
	planet = next_planet
	player.planet = next_planet
	player.get_node(\"Scarf\").planet = next_planet
	game_viewport.remove_child(previous_planet)
	previous_planet.free()
	next_planet.teleport_player(next_planet.spawn_angle)
	player.modulate.a = 1.0
	%GameCamera.offset = Vector2.ZERO
	%Epilogue.text = \"\"
	%MigratoryFlock.visible = false
	%MigratoryFlock.set_process(false)


func _input(event: InputEvent) -> void:
	var control_key := event as InputEventKey
	if OS.is_debug_build() and control_key != null and not control_key.is_echo() and control_key.physical_keycode == KEY_CTRL and control_key.location == KEY_LOCATION_LEFT:
		Engine.time_scale = WorldConstants.FAST_FORWARD_TIME_SCALE if control_key.pressed else 1.0
	if not dialogue.is_open():
		return
	get_viewport().set_input_as_handled()
	if event.is_echo():
		return
	if not event.is_action(&\"interact\") and not (
			event is InputEventKey
			or event is InputEventJoypadButton
			or event is InputEventMouseButton
	):
		return
	if event.is_pressed():
		dialogue.mark_holding(true)
		return
	var key := event as InputEventKey
	if key != null and key.physical_keycode != KEY_NONE and Input.is_physical_key_pressed(key.physical_keycode):
		return
	var joy_button := event as InputEventJoypadButton
	if joy_button != null and Input.is_joy_button_pressed(joy_button.device, joy_button.button_index):
		return
	var mouse_button := event as InputEventMouseButton
	if mouse_button != null and Input.is_mouse_button_pressed(mouse_button.button_index):
		return
	dialogue.mark_holding(false)
"

[sub_resource type="GDScript" id="GDScript_player"]
script/source = "extends Sprite2D
## 小王子：左右输入驱动星球旋转，自身钉在弧顶。
## 贴图为 idle+walk 横拼 spritesheet，按角速度切帧；左右用 flip_h。

@onready var planet = %Planet

var can_move_left: bool = true
var can_move_right: bool = true
var move_speed_scale: float = 1.0
var _anim_time: float = 0.0
var _was_moving: bool = false
var _last_walk_frame_index: int = -1

func _physics_process(delta: float) -> void:
	var direction := 0.0
	if is_busy():
		planet.angular_velocity = 0.0
	else:
		if can_move_right:
			direction += Input.get_action_strength(\"move_right\")
		if can_move_left:
			direction -= Input.get_action_strength(\"move_left\")
		direction *= move_speed_scale
	planet.move_player(direction, delta)
	planet.refresh_interact_focus()
	_update_animation(direction, delta)

func is_busy() -> bool:
	var story := planet.get_node(\"%Story\") as PlanetStory
	return story.is_blocking_player_input() or planet.is_interact_holding

func is_in_grass() -> bool:
	return planet.is_player_in_grass()


func find_nearest_interactable(should_accept: Callable = Callable()) -> SurfaceProp:
	if planet.focused_player_trigger == null:
		return null
	var prop := planet.focused_player_trigger.hosted_surface_prop() as SurfaceProp
	if prop == null:
		return null
	if should_accept.is_valid() and not should_accept.call(prop):
		return null
	return prop


func _update_animation(direction: float, delta: float) -> void:
	if absf(direction) > 0.01:
		flip_h = direction < 0.0
	var moving: bool = planet.is_moving()
	if moving != _was_moving:
		_anim_time = 0.0
		_last_walk_frame_index = -1
		if not moving:
			%Footstep.stop()
		_was_moving = moving
	_anim_time += delta
	if moving:
		var walk_count := WorldConstants.PLAYER_WALK_FRAME_COUNT
		var walk_index := int(_anim_time * WorldConstants.PLAYER_WALK_FPS) % walk_count
		frame = WorldConstants.PLAYER_IDLE_FRAME_COUNT + walk_index
		if walk_index != _last_walk_frame_index:
			_last_walk_frame_index = walk_index
			if walk_index % 2 == 0 and not is_busy():
				%Footstep.play_step()
	else:
		var idle_count := WorldConstants.PLAYER_IDLE_FRAME_COUNT
		var idx := int(_anim_time * WorldConstants.PLAYER_IDLE_FPS) % idle_count
		frame = idx
"

[sub_resource type="GDScript" id="GDScript_scarf"]
script/source = "extends Sprite2D
## 小王子围巾：在玩家本地空间做 Verlet 绳。
## 人钉在弧顶、行走靠星球反转，引擎刚体没有表观惯性，所以不用物理体。
## 模拟每物理帧更新，显示烘焙成小贴图并取整，做成像素抽帧。

const POINT_COUNT: int = 7
const SEGMENT_LENGTH: float = 1.7
const GRAVITY: float = 180.0
const DAMPING: float = 0.86
const WIND_PER_WALK_SPEED: float = 22.0
const IDLE_SWAY: float = 36.0
const CONSTRAINT_ITERATIONS: int = 5
const DISPLAY_FPS: float = 8.0
const NECK_SIDE_OFFSET: float = 2.0
const NECK_LOCAL_Y: float = -9.0
const MIN_LOCAL_Y: float = -15.0
const MAX_LOCAL_Y: float = -3.0
const FRAME_WIDTH: int = 32
const FRAME_HEIGHT: int = 24
const FRAME_ORIGIN := Vector2i(-16, -16)
const COLOR := Color8(224, 171, 74)
const SHADOW_COLOR := Color8(224, 171, 74)

@onready var planet = %Planet
@onready var player = %Player

var simulated_positions: PackedVector2Array
var previous_positions: PackedVector2Array
var display_positions: PackedVector2Array
var _display_accumulator: float = 0.0
var _frame_image: Image
var _frame_texture: ImageTexture


func _ready() -> void:
	_frame_image = Image.create(FRAME_WIDTH, FRAME_HEIGHT, false, Image.FORMAT_RGBA8)
	_frame_texture = ImageTexture.create_from_image(_frame_image)
	texture = _frame_texture
	settle_hang_to_facing()


func _physics_process(delta: float) -> void:
	_simulate(delta)
	_display_accumulator += delta
	var display_interval := 1.0 / DISPLAY_FPS
	if _display_accumulator >= display_interval:
		_display_accumulator = fmod(_display_accumulator, display_interval)
		_capture_display()


func settle_hang_to_facing() -> void:
	var neck := _neck_local_position()
	var hang_direction := Vector2(-_facing_sign(), 1.2).normalized()
	simulated_positions.resize(POINT_COUNT)
	previous_positions.resize(POINT_COUNT)
	display_positions.resize(POINT_COUNT)
	for point_index in POINT_COUNT:
		var point := neck + hang_direction * SEGMENT_LENGTH * float(point_index)
		simulated_positions[point_index] = point
		previous_positions[point_index] = point
	for _settle_step in 24:
		_simulate(1.0 / 60.0)
	_capture_display()


func _simulate(delta: float) -> void:
	_pin_neck()
	var walk_speed: float = planet.angular_velocity * planet.radius
	var motion_weight := clampf(absf(walk_speed) / WorldConstants.PLAYER_SPEED, 0.0, 1.0)
	var wind_x := lerpf(
			-_facing_sign() * IDLE_SWAY,
			-walk_speed * WIND_PER_WALK_SPEED,
			motion_weight
	)
	var acceleration := Vector2(wind_x, GRAVITY)
	var delta_squared := delta * delta
	for point_index in range(1, POINT_COUNT):
		var current_position := simulated_positions[point_index]
		var previous_position := previous_positions[point_index]
		var next_position := (
				current_position
				+ (current_position - previous_position) * DAMPING
				+ acceleration * delta_squared
		)
		previous_positions[point_index] = current_position
		simulated_positions[point_index] = next_position
	for _constraint_pass in CONSTRAINT_ITERATIONS:
		for point_index in range(1, POINT_COUNT):
			var from_position := simulated_positions[point_index - 1]
			var to_position := simulated_positions[point_index]
			var segment_offset := to_position - from_position
			var distance := segment_offset.length()
			if distance < 0.001:
				segment_offset = Vector2(0.0, 1.0)
				distance = 1.0
			var correction := segment_offset * (1.0 - SEGMENT_LENGTH / distance)
			if point_index == 1:
				simulated_positions[point_index] = to_position - correction
			else:
				simulated_positions[point_index - 1] = from_position + correction * 0.5
				simulated_positions[point_index] = to_position - correction * 0.5
		_pin_neck()
		for point_index in range(1, POINT_COUNT):
			var clamped := simulated_positions[point_index]
			clamped.y = clampf(clamped.y, MIN_LOCAL_Y, MAX_LOCAL_Y)
			simulated_positions[point_index] = clamped


func _capture_display() -> void:
	display_positions.resize(POINT_COUNT)
	for point_index in POINT_COUNT:
		display_positions[point_index] = simulated_positions[point_index].round()
	_frame_image.fill(Color(0, 0, 0, 0))
	var knot := Vector2i(display_positions[0]) - FRAME_ORIGIN
	_plot_dot(knot + Vector2i(0, 1), SHADOW_COLOR, 2)
	_plot_dot(knot, COLOR, 2)
	for point_index in range(display_positions.size() - 1):
		var thickness := 2 if point_index < 3 else 1
		_plot_line(
				Vector2i(display_positions[point_index]) - FRAME_ORIGIN,
				Vector2i(display_positions[point_index + 1]) - FRAME_ORIGIN,
				thickness
		)
	_frame_texture.update(_frame_image)


func _pin_neck() -> void:
	var neck := _neck_local_position()
	simulated_positions[0] = neck
	previous_positions[0] = neck


func _neck_local_position() -> Vector2:
	var walk_index: int = player.frame - WorldConstants.PLAYER_IDLE_FRAME_COUNT
	var body_bob := 1.0 if walk_index >= 0 and walk_index % 2 == 0 else 0.0
	return Vector2(NECK_SIDE_OFFSET * -_facing_sign(), NECK_LOCAL_Y + body_bob)


func _facing_sign() -> float:
	return -1.0 if player.flip_h else 1.0


func _plot_line(from_pixel: Vector2i, to_pixel: Vector2i, thickness: int) -> void:
	var delta_x := absi(to_pixel.x - from_pixel.x)
	var delta_y := absi(to_pixel.y - from_pixel.y)
	var step_x := 1 if from_pixel.x < to_pixel.x else -1
	var step_y := 1 if from_pixel.y < to_pixel.y else -1
	var error := delta_x - delta_y
	var pixel := from_pixel
	while true:
		_plot_dot(pixel + Vector2i(0, 1), SHADOW_COLOR, thickness)
		_plot_dot(pixel, COLOR, thickness)
		if pixel == to_pixel:
			break
		var doubled_error := error * 2
		if doubled_error > -delta_y:
			error -= delta_y
			pixel.x += step_x
		if doubled_error < delta_x:
			error += delta_x
			pixel.y += step_y


func _plot_dot(pixel: Vector2i, color: Color, thickness: int) -> void:
	for offset_x in thickness:
		var image_x := pixel.x + offset_x
		if image_x < 0 or pixel.y < 0 or image_x >= FRAME_WIDTH or pixel.y >= FRAME_HEIGHT:
			continue
		_frame_image.set_pixel(image_x, pixel.y, color)
"

[sub_resource type="GDScript" id="GDScript_flock"]
script/source = "extends Node2D
## 候鸟：离星时从屏幕外飞到弧顶，再向上带走小王子。

const BIRD_COUNT := 7
const ARRIVE_SECONDS := 1.4
const WING_FRAME_MILLISECONDS := 140

@export var bird_texture: Texture2D

var _birds: Array[Sprite2D] = []


func _ready() -> void:
	visible = false
	set_process(false)
	for bird_index in BIRD_COUNT:
		var bird := Sprite2D.new()
		bird.texture = bird_texture
		bird.hframes = 2
		bird.texture_filter = TEXTURE_FILTER_NEAREST
		bird.z_index = 180
		add_child(bird)
		_birds.append(bird)


func arrive_from_offscreen(apex_global_position: Vector2) -> void:
	visible = true
	set_process(true)
	var viewport_rect := get_viewport().get_visible_rect()
	const offscreen_margin := 28.0
	for bird_index in _birds.size():
		var from_left := bird_index % 2 == 0
		var bird := _birds[bird_index]
		var spawn_x := (
				viewport_rect.position.x - offscreen_margin - float(bird_index) * 10.0
				if from_left
				else viewport_rect.end.x + offscreen_margin + float(bird_index) * 10.0
		)
		bird.global_position = Vector2(
				spawn_x,
				viewport_rect.position.y - offscreen_margin - float(bird_index) * 8.0
		)
		bird.flip_h = not from_left
	var gather_tween := create_tween().set_parallel(true)
	for bird_index in _birds.size():
		var scatter := Vector2.from_angle(float(bird_index) * 0.9) * 12.0
		gather_tween.tween_property(
				_birds[bird_index],
				\"global_position\",
				apex_global_position + scatter,
				ARRIVE_SECONDS
		)
	await gather_tween.finished


func lift(distance_pixels: float, duration_seconds: float) -> void:
	var lift_tween := create_tween().set_parallel(true)
	for bird in _birds:
		lift_tween.tween_property(
				bird,
				\"global_position:y\",
				bird.global_position.y - distance_pixels,
				duration_seconds
		)


func _process(_delta: float) -> void:
	var wing_frame := int(Time.get_ticks_msec() / WING_FRAME_MILLISECONDS) % 2
	for bird in _birds:
		bird.frame = wing_frame
"



[sub_resource type="Texture2D" id="EditableTexture_prince"]
script = ExtResource("editable_texture_script")
_base64_data = ""

[sub_resource type="Texture2D" id="EditableTexture_migratory_bird"]
script = ExtResource("editable_texture_script")
_base64_data = "iVBORw0KGgoAAAANSUhEUgAAABAAAAAICAYAAADwdn+XAAAAZUlEQVR4nGNgQALa6i7/tdVd/jOQABhhGhkYGBiu3tyDlY8sBgMwOUaYxNuvn1BMFubmQ+Ejyz+/PZtBx6AY4QIGBgYGCRmz/8gKGDn1MeRePDnFiI3PgKwQ2SB0gE2eCZfioQMAUJwraTZSGawAAAAASUVORK5CYII="

[sub_resource type="GDScript" id="GDScript_footstep"]
script/source = "extends AudioStreamPlayer
## 行走落地 one-shot：默认土地；进草丛 Area 后改播草地。

const META_DIRT_FOOTSTEP_A := \"dirt_footstep_a\"
const META_DIRT_FOOTSTEP_B := \"dirt_footstep_b\"
const META_GRASS_FOOTSTEP_A := \"grass_footstep_a\"
const META_GRASS_FOOTSTEP_B := \"grass_footstep_b\"

var played_step_count: int = 0
var last_step_was_grass: bool = false


func play_step() -> void:
	var variant_is_first := (played_step_count % 2) == 0
	played_step_count += 1
	var use_grass: bool = %Player.is_in_grass()
	var meta_name: String
	if use_grass:
		meta_name = META_GRASS_FOOTSTEP_A if variant_is_first else META_GRASS_FOOTSTEP_B
	else:
		meta_name = META_DIRT_FOOTSTEP_A if variant_is_first else META_DIRT_FOOTSTEP_B
	stream = owner.get_node(\"Config\").get_meta(meta_name) as AudioStream
	last_step_was_grass = use_grass
	play()
"

[sub_resource type="GDScript" id="GDScript_music"]
script/source = "extends Node
## 循环配乐：故乡、国王、酒鬼、商人、点灯人、地理学家星球，交叉淡化切换。

const META_HOME_DAY_MUSIC := \"home_day_music\"
const META_KING_DAY_MUSIC := \"king_day_music\"
const META_DRUNKARD_DAY_MUSIC := \"drunkard_day_music\"
const META_MERCHANT_DAY_MUSIC := \"merchant_day_music\"
const META_LAMPLIGHTER_DAY_MUSIC := \"lamplighter_day_music\"
const META_GEOGRAPHER_DAY_MUSIC := \"geographer_day_music\"
const META_OPENING_DAY_MUSIC := \"opening_day_music\"
const CROSSFADE_SECONDS := 1.6
const PLAYING_VOLUME_DB := -10.0
const SILENT_VOLUME_DB := -80.0

var playing_stream: AudioStream:
	get:
		return _active.stream

var _active: AudioStreamPlayer
var _idle: AudioStreamPlayer
var _fade_tween: Tween


func _ready() -> void:
	_active = $Playing
	_idle = $Incoming
	for meta_name in [META_HOME_DAY_MUSIC, META_KING_DAY_MUSIC]:
		(_config_music(meta_name) as AudioStreamOggVorbis).loop = true
	var config := owner.get_node(\"Config\")
	if config.has_meta(META_DRUNKARD_DAY_MUSIC):
		(_config_music(META_DRUNKARD_DAY_MUSIC) as AudioStreamOggVorbis).loop = true
	if config.has_meta(META_MERCHANT_DAY_MUSIC):
		(_config_music(META_MERCHANT_DAY_MUSIC) as AudioStreamOggVorbis).loop = true
	if config.has_meta(META_LAMPLIGHTER_DAY_MUSIC):
		(_config_music(META_LAMPLIGHTER_DAY_MUSIC) as AudioStreamOggVorbis).loop = true
	if config.has_meta(META_GEOGRAPHER_DAY_MUSIC):
		(_config_music(META_GEOGRAPHER_DAY_MUSIC) as AudioStreamOggVorbis).loop = true
	_active.volume_db = SILENT_VOLUME_DB
	_idle.volume_db = SILENT_VOLUME_DB
	_active.stream = _opening_music()
	_active.play()
	_fade_tween = create_tween()
	_fade_tween.tween_property(_active, \"volume_db\", PLAYING_VOLUME_DB, CROSSFADE_SECONDS)


func play_king_day_loop() -> void:
	_crossfade_to(_config_music(META_KING_DAY_MUSIC))


func play_drunkard_day_loop() -> void:
	_crossfade_to(_config_music(META_DRUNKARD_DAY_MUSIC))


func play_merchant_day_loop() -> void:
	_crossfade_to(_config_music(META_MERCHANT_DAY_MUSIC))


func play_lamplighter_day_loop() -> void:
	_crossfade_to(_config_music(META_LAMPLIGHTER_DAY_MUSIC))


func play_geographer_day_loop() -> void:
	_crossfade_to(_config_music(META_GEOGRAPHER_DAY_MUSIC))


func fade_out(duration_seconds: float = CROSSFADE_SECONDS) -> void:
	var a := _active
	var b := _idle
	if _fade_tween != null:
		_fade_tween.kill()
	_fade_tween = create_tween()
	_fade_tween.set_parallel(true)
	_fade_tween.tween_property(a, \"volume_db\", SILENT_VOLUME_DB, duration_seconds)
	_fade_tween.tween_property(b, \"volume_db\", SILENT_VOLUME_DB, duration_seconds)
	_fade_tween.chain().tween_callback(a.stop)
	_fade_tween.chain().tween_callback(b.stop)


func _opening_music() -> AudioStream:
	var config := owner.get_node(\"Config\")
	if config.has_meta(META_OPENING_DAY_MUSIC):
		var opening := config.get_meta(META_OPENING_DAY_MUSIC) as AudioStream
		var vorbis := opening as AudioStreamOggVorbis
		if vorbis != null:
			vorbis.loop = true
		return opening
	return _config_music(META_HOME_DAY_MUSIC)


func _crossfade_to(music: AudioStream, duration_seconds: float = CROSSFADE_SECONDS) -> void:
	if music != null and _active.stream == music and _active.playing:
		return
	if _fade_tween != null:
		_fade_tween.kill()
	var outgoing := _active
	var incoming := _idle
	_active = incoming
	_idle = outgoing
	incoming.stop()
	incoming.stream = music
	incoming.volume_db = SILENT_VOLUME_DB
	if music != null:
		incoming.play()
	_fade_tween = create_tween()
	_fade_tween.set_parallel(true)
	_fade_tween.tween_property(outgoing, \"volume_db\", SILENT_VOLUME_DB, duration_seconds)
	if music != null:
		_fade_tween.tween_property(incoming, \"volume_db\", PLAYING_VOLUME_DB, duration_seconds)
	_fade_tween.chain().tween_callback(outgoing.stop)


func _config_music(meta_name: String) -> AudioStream:
	return owner.get_node(\"Config\").get_meta(meta_name) as AudioStream
"

[sub_resource type="ShaderMaterial" id="ShaderMaterial_palette"]
shader = SubResource("Shader_palette")
shader_parameter/palette = SubResource("EditableTexture_palette")

[sub_resource type="CircleShape2D" id="CircleShape2D_footprint"]
radius = 6.0

[node name="PlanetRunShell" type="Control" unique_id=134579991]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = SubResource("GDScript_shell")

[node name="Config" type="Node" parent="."]
process_mode = 4
metadata/home_day_music = ExtResource("19_home_day_music")
metadata/king_day_music = ExtResource("21_king_day_music")
metadata/dirt_footstep_a = ExtResource("22_dirt_a")
metadata/dirt_footstep_b = ExtResource("23_dirt_b")
metadata/grass_footstep_a = ExtResource("24_grass_a")
metadata/grass_footstep_b = ExtResource("25_grass_b")

[node name="Music" type="Node" parent="."]
unique_name_in_owner = true
script = SubResource("GDScript_music")

[node name="Playing" type="AudioStreamPlayer" parent="Music"]
bus = &"Music"

[node name="Incoming" type="AudioStreamPlayer" parent="Music"]
bus = &"Music"

[node name="GameView" type="SubViewportContainer" parent="." unique_id=1849713084]
texture_filter = 1
material = SubResource("ShaderMaterial_palette")
layout_mode = 1
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
stretch = true

[node name="GameViewport" type="SubViewport" parent="GameView" unique_id=877318439]
handle_input_locally = false
snap_2d_transforms_to_pixel = true
snap_2d_vertices_to_pixel = true
canvas_item_default_texture_filter = 0
size = Vector2i(256, 224)
render_target_update_mode = 4

[node name="GameCamera" type="Camera2D" parent="GameView/GameViewport"]
unique_name_in_owner = true
anchor_mode = 0

[node name="Player" type="Sprite2D" parent="GameView/GameViewport" unique_id=232581758]
unique_name_in_owner = true
z_index = 200
position = Vector2(128, 197.12)
texture_filter = 1
texture = SubResource("EditableTexture_prince")
offset = Vector2(0, -7)
flip_h = true
hframes = 6
script = SubResource("GDScript_player")

[node name="Footprint" type="Area2D" parent="GameView/GameViewport/Player"]
unique_name_in_owner = true
collision_layer = 1
collision_mask = 0
monitoring = false
input_pickable = false

[node name="CollisionShape2D" type="CollisionShape2D" parent="GameView/GameViewport/Player/Footprint"]
shape = SubResource("CircleShape2D_footprint")

[node name="Footstep" type="AudioStreamPlayer" parent="GameView/GameViewport/Player"]
unique_name_in_owner = true
volume_db = -20.9151
script = SubResource("GDScript_footstep")

[node name="Scarf" type="Sprite2D" parent="GameView/GameViewport/Player" unique_id=1847293651]
unique_name_in_owner = true
z_index = 1
texture_filter = 1
centered = false
offset = Vector2(-16, -16)
script = SubResource("GDScript_scarf")


[node name="DialogueBox" parent="GameView/GameViewport" unique_id=1685094322 instance=ExtResource("9_dialogue")]
unique_name_in_owner = true

[node name="OverheadTypewriter" parent="GameView/GameViewport" unique_id=1992303185 instance=ExtResource("12_overhead")]
unique_name_in_owner = true


[node name="MigratoryFlock" type="Node2D" parent="GameView/GameViewport" unique_id=515559498]
unique_name_in_owner = true
z_index = 180
script = SubResource("GDScript_flock")
bird_texture = SubResource("EditableTexture_migratory_bird")

[node name="FadeLayer" type="CanvasLayer" parent="GameView/GameViewport" unique_id=1234713668]
layer = 5

[node name="Dim" type="ColorRect" parent="GameView/GameViewport/FadeLayer" unique_id=1388500103]
unique_name_in_owner = true
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
mouse_filter = 2
color = Color(0, 0, 0, 0)

[node name="Epilogue" type="Label" parent="GameView/GameViewport/FadeLayer" unique_id=222522644]
unique_name_in_owner = true
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
theme_override_colors/font_color = Color(1, 0.968627, 0.819608, 1)
theme_override_fonts/font = ExtResource("15_font")
theme_override_font_sizes/font_size = 8
horizontal_alignment = 1
vertical_alignment = 1
